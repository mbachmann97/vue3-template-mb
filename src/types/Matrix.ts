export type MatrixPosition = string

export interface MatrixOptions {
  size: string,
  color: string,
  labelSize: string,
}

export interface MatrixLabelPropType {
  individual: string[],
  axis: string[],
}